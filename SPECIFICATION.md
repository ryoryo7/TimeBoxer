# タスク管理アプリ（TimeBoxer）仕様書

## 概要

シンプルな静的タスク管理アプリケーション。LocalStorageとGoogleスプレッドシートを使用してデータを永続化する。タイマー機能、実行ログ機能、メモ帳機能、グラフ表示機能を備える。

## ファイル構成

```
TimeBoxer/
├── index.html          # HTML構造
├── style.css           # スタイルシート
├── script.js           # JavaScript ロジック
├── 成功音.mp3          # タイマー完了時の通知音
├── package.json        # npm設定
├── .prettierrc         # Prettier設定
├── .prettierignore     # Prettier除外設定
├── eslint.config.js    # ESLint設定
├── vitest.config.js    # Vitest設定
├── .gitignore          # Git除外設定
├── SPECIFICATION.md    # 仕様書（本ファイル）
├── SETUP_GUIDE.md      # 開発環境セットアップ指示書
└── tests/
    └── tasks.test.js   # ユニットテスト
```

## 技術仕様

| 項目 | 内容 |
|------|------|
| フロントエンド | HTML5, CSS3, Vanilla JavaScript |
| データ保存（ローカル） | LocalStorage |
| データ保存（クラウド） | Google スプレッドシート + Google Apps Script |
| グラフ描画 | Chart.js（CDN） |
| テスト | Vitest |
| コード整形 | Prettier |
| 静的解析 | ESLint |
| レスポンシブ | 対応（768px, 480px ブレークポイント） |

---

## データ保存

### LocalStorageキー

| キー | 内容 |
|------|------|
| `taskManager_tasks` | タスクデータ |
| `taskManager_logs` | 実行ログデータ |
| `taskManager_memos` | メモ帳データ |
| `taskManager_timer` | タイマー状態（リロード時復元用） |

### Googleスプレッドシート連携

| シート名 | 内容 |
|----------|------|
| `tasks` | タスクデータ |
| `logs` | 実行ログデータ |
| `memos` | メモ帳データ |

#### 同期タイミング
- アプリ起動時: Googleスプレッドシート → LocalStorage（読み込み）
- データ変更時: LocalStorage → Googleスプレッドシート（書き込み）

#### オフライン対応
- Googleスプレッドシートに接続できない場合はLocalStorageのみで動作

---

## データ構造

### タスクオブジェクト

```javascript
{
    id: number,            // ユニークID（Date.now()で生成）
    name: string,          // タスク名
    priority: number,      // 優先順位（1以上の整数、一意、小さいほど高優先）
    estimatedTime: number, // 想定作業時間（分単位）
    targetDate: string,    // 対象日（YYYY-MM-DD形式）
    completed: boolean,    // 完了状態
    createdAt: string      // 作成日時（ISO 8601形式）
}
```

### ログオブジェクト

```javascript
{
    id: number,              // ユニークID（Date.now()で生成）
    taskId: number|string,   // 対象タスクのID（割り込みタスクの場合は'interrupt'）
    taskName: string,        // タスク名（タスク削除後も表示するため保持）
    taskDetail: string,      // タスク内容詳細（任意）
    achievement: number,     // 達成度（0-100、任意）
    estimatedTime: number,   // 想定時間（分、割り込みタスクの場合は入力時間）
    startDateTime: string,   // 開始日時（YYYY-MM-DD HH:MM:SS形式）
    endDateTime: string,     // 終了日時（YYYY-MM-DD HH:MM:SS形式）
    duration: number,        // 実行時間（分、小数点1桁）
    completed: boolean       // 最後まで完了したか（true=完了、false=中断）
}
```

### メモオブジェクト

```javascript
// LocalStorage形式
{
    memos: [
        { id: number, name: string, content: string }
    ],
    activeTabId: number,
    isCollapsed: boolean
}

// Googleスプレッドシート形式（各メモを1行ずつ保存）
{
    id: number,
    name: string,
    content: string
}
```

### タイマー状態オブジェクト（リロード復元用）

```javascript
{
    isRunning: boolean,
    startTime: string,       // ISO 8601形式
    endTime: string,         // ISO 8601形式
    totalSeconds: number,
    taskId: number|string,
    taskName: string,
    taskDetail: string,
    achievement: number,
    estimatedTime: number
}
```

---

## 画面構成

### 1. メモ帳セクション

| 要素 | 説明 |
|------|------|
| タブバー | 複数メモをタブで切り替え |
| タブ追加ボタン | 新規メモ追加（最大10個） |
| タブ名編集 | ダブルクリックで編集可能 |
| タブ削除 | ×ボタンで削除（最低1つは残す） |
| テキストエリア | メモ内容入力 |
| 折りたたみ | ヘッダークリックで開閉 |

### 2. 統計エリア

| 表示項目 | 説明 |
|----------|------|
| 総タスク数 | 登録されている全タスク数 |
| 未完了 | 完了していないタスク数 |
| 総想定時間 | 未完了タスクの想定時間合計 |

### 3. 入力フォーム

| フィールド | 型 | デフォルト値 | 制約 |
|------------|-----|--------------|------|
| タスク名 | text | - | 必須、空文字不可 |
| 優先順位 | number | 空（`--`） | 1以上の整数、未指定で最下位 |
| 想定時間(分) | number | 30 | 1以上の整数 |
| 対象日 | date | 今日 | - |

### 4. タスク一覧テーブル

| カラム | 説明 |
|--------|------|
| ドラッグハンドル | 並び替え用（⋮⋮） |
| チェックボックス | タスク選択用 |
| 優先 | 優先順位（バッジ表示） |
| タスク名 | タスクの名称 |
| 想定時間 | 想定作業時間 |
| 対象日 | タスク実施予定日（M/D形式） |
| 操作 | 完了/編集/削除ボタン |

### 5. ツールバー（タスクリスト下部）

| ボタン | 機能 |
|--------|------|
| CSVエクスポート | タスク + ログをCSV形式でダウンロード |
| 選択削除 | チェックしたタスクを削除 |
| 全削除 | 全タスクを削除 |

### 6. タイマーセクション

| 要素 | 説明 |
|------|------|
| タスク選択 | ドロップダウン（デフォルト: 割り込みタスク） |
| タスク内容詳細 | テキストエリア（任意） |
| 達成度 | 0〜100の数値入力（任意） |
| プリセットボタン | 5分/10分/30分/60分 |
| 時間調整ボタン | -10分/-1分/+1分/+5分/+10分/+30分/+60分 |
| カスタム入力 | 任意の分数を入力（デフォルト: 5分） |
| タイマー表示 | 残り時間（MM:SS形式） |
| スケジュール表示 | 開始時刻、終了予定時刻 |
| スタートボタン | カウントダウン開始 |
| ストップボタン | 一時停止（ログ保存） |
| クリアボタン | タイマーリセット |

### 7. 実行ログセクション

| 要素 | 説明 |
|------|------|
| 合計経過時間 | 全ログの実行時間合計 |
| 件数 | ログの総件数 |

| カラム | 説明 | 編集可否 |
|--------|------|----------|
| 開始日時 | YYYY-MM-DD HH:MM:SS形式 | ✅ |
| 終了日時 | YYYY-MM-DD HH:MM:SS形式 | ✅ |
| タスク名 | 実行したタスク名（ドロップダウン選択） | ✅ |
| 詳細 | タスク内容詳細 | ✅ |
| 想定時間 | タスクに設定された想定時間 | ✅ |
| 実行時間 | 今回の経過時間（自動再計算） | ✅ |
| 合計 | そのタスクの累計実行時間（自動計算） | ❌ |
| 達成度 | 0-100%（任意） | ✅ |
| 超過時間 | 合計 - 想定時間（自動計算） | ❌ |
| 操作 | 編集/削除ボタン | - |

| ボタン | 機能 |
|--------|------|
| ログCSV出力 | 実行ログをCSV形式でダウンロード |
| ログ全削除 | 全ログを削除 |

### 8. タスク別時間割合グラフ

| 要素 | 説明 |
|------|------|
| 期間フィルター | 今日/今週/今月/全期間 |
| 円グラフ | タスク別の実行時間割合 |
| 凡例 | タスク名と色 |
| ツールチップ | ホバーで詳細表示 |

### 9. 活動タイムライン

| 要素 | 説明 |
|------|------|
| 表示切替ボタン | タイムラインの表示/非表示 |
| 期間フィルター | 今日/今週/今月/全期間 |
| タイムラインバー | 各ログを横軸で表示 |
| 時間軸 | 下部に時刻ラベルを表示 |
| ツールチップ | ホバーで詳細表示 |
| 拡大モーダル | クリックで拡大表示 |

---

## 機能一覧

### タスク管理機能

| 機能 | 説明 |
|------|------|
| タスク追加 | フォームから新規タスク追加 |
| 優先順位の一意性 | 1から連番で自動管理 |
| ドラッグ&ドロップ | 未完了タスクの並び替え |
| 完了切り替え | チェックで完了/未完了切り替え |
| インライン編集 | 編集ボタンで直接編集 |
| タスク削除 | 個別/選択/全削除 |

### タイマー機能

| 機能 | 説明 |
|------|------|
| タスク選択 | 未完了タスクまたは割り込みタスク |
| プリセット時間 | 5分/10分/30分/60分 |
| 時間調整 | -10/-1/+1/+5/+10/+30/+60分 |
| カスタム時間 | 1〜999分の任意入力 |
| リロード復元 | カウントダウン中にリロードしても継続 |
| 通知 | 完了時に通知音（繰り返し再生）、ブラウザ通知 |

### 実行ログ機能

| 機能 | 説明 |
|------|------|
| 自動保存 | タイマー完了/停止時にログ保存 |
| インライン編集 | 各項目を直接編集可能 |
| 実行時間再計算 | 日時編集時に自動再計算 |
| 超過時間表示 | 想定時間との差分を自動計算 |
| CSV出力 | ログのみを別ファイルで出力 |
| 全削除 | 全ログを一括削除 |

### メモ帳機能

| 機能 | 説明 |
|------|------|
| 複数タブ | 最大10個のメモを管理 |
| タブ追加/削除 | 動的にタブを増減 |
| タブ名編集 | ダブルクリックで編集 |
| 折りたたみ | ヘッダークリックで開閉 |
| 自動保存 | 入力内容を即時保存 |

### グラフ・タイムライン機能

| 機能 | 説明 |
|------|------|
| 円グラフ | タスク別時間割合を表示 |
| 期間フィルター | 今日/今週/今月/全期間 |
| 1%未満集約 | 小さい割合は「その他」に統合 |
| タイムライン | 活動を時系列で表示 |
| 重複回避 | バーが重ならないよう自動配置 |
| 拡大表示 | モーダルで詳細確認 |

---

## CSVエクスポート

### タスク+ログCSV（CSVエクスポートボタン）

ファイル名: `tasks_YYYYMMDD_HHMMSS.csv`

```csv
=== タスク一覧 ===
タスク名,優先順位,想定時間(分),対象日,完了,作成日

=== 実行ログ ===
開始日時,終了日時,タスク名,詳細,想定時間(分),実行時間(分),タスク合計(分),達成度(%)
```

### ログCSV（ログCSV出力ボタン）

ファイル名: `logs_YYYYMMDD_HHMMSS.csv`

```csv
開始日時,終了日時,タスク名,詳細,想定時間(分),実行時間(分),タスク合計(分),達成度(%),超過時間(分)
```

---

## 表示仕様

### 対象日の色分け

| 状態 | 色 | CSSクラス |
|------|-----|-----------|
| 過去 | 赤 | `.past` |
| 今日 | 緑（太字） | `.today` |
| 未来 | 黒 | - |

### 優先順位バッジの色分け

| 位置 | 色 | CSSクラス |
|------|-----|-----------|
| 上位1/3 | 赤系 | `.priority-high` |
| 中位1/3 | オレンジ系 | `.priority-medium` |
| 下位1/3 | 緑系 | `.priority-low` |

### 超過時間の表示

| 状態 | 表示 | 色 |
|------|------|-----|
| 想定内 | `-X分` または `0分` | 黒 |
| 超過 | `+X分` | 赤 |
| 想定時間なし | `-` | 灰色 |

### 時間軸ラベルの間隔

| 表示期間 | 間隔 |
|----------|------|
| 1時間以内 | 10分 |
| 2時間以内 | 15分 |
| 4時間以内 | 30分 |
| 8時間以内 | 60分 |
| 8時間超 | 120分 |

---

## 状態遷移

### タイマー状態

```
[初期状態] (割り込みタスク選択済み)
    │
    ├─ スタート → [実行中] ─┬─ 時間経過 → [完了] → ログ保存 → [初期状態]
    │                       │
    │                       ├─ ストップ → ログ保存 → [停止中]
    │                       │               │
    │                       │               ├─ スタート → [実行中]
    │                       │               │
    │                       │               └─ クリア → [初期状態]
    │                       │
    │                       └─ リロード → [実行中] (復元)
    │
    └─ クリア → [初期状態]
```

---

## 制約事項

### タスク

| 項目 | 制約 |
|------|------|
| タスク名 | 必須、空文字不可 |
| 優先順位 | 1以上の整数、一意 |
| 想定時間 | 1分以上 |

### タイマー

| 項目 | 制約 |
|------|------|
| 時間設定 | 1〜999分 |
| 最小値 | 1分 |
| 最大値 | 999分 |

### メモ帳

| 項目 | 制約 |
|------|------|
| タブ数 | 最大10個 |
| 最小タブ数 | 1個（削除不可） |

### 達成度

| 項目 | 制約 |
|------|------|
| 入力範囲 | 0〜100 |
| 任意入力 | 空欄可 |

---

## Google Apps Script API

### エンドポイント

```
https://script.google.com/macros/s/[DEPLOYMENT_ID]/exec
```

### パラメータ

| action | 説明 |
|--------|------|
| `readAll` | 全シートのデータを取得 |
| `write` | 指定シートにデータを書き込み |
| `writeAll` | 全シートにデータを書き込み |

### レスポンス形式

```javascript
// readAll
{
    tasks: [...],
    logs: [...],
    memos: [...]
}

// write / writeAll
{
    success: true,
    count: number
}
```

---

## 更新履歴

| バージョン | 日付 | 内容 |
|------------|------|------|
| 1.0.0 | - | 初回リリース |
| 1.1.0 | - | 想定時間を分単位に変更 |
| 1.2.0 | - | CSVエクスポート、選択削除、全削除機能追加 |
| 1.3.0 | - | 対象日フィールド追加 |
| 1.4.0 | - | インライン編集機能追加 |
| 1.5.0 | - | 優先順位の一意化、ドラッグ&ドロップ並び替え |
| 1.6.0 | - | タイマー機能、実行ログ機能追加 |
| 1.7.0 | - | 割り込みタスクをデフォルトに、ストップ時ログ保存 |
| 1.8.0 | - | メモ帳機能追加 |
| 1.9.0 | - | タスク別時間割合グラフ追加 |
| 2.0.0 | - | 活動タイムライン追加 |
| 2.1.0 | - | ログ編集機能（インライン編集、タスク選択、実行時間再計算） |
| 2.2.0 | - | 超過時間表示、ログサマリー表示 |
| 2.3.0 | - | Googleスプレッドシート連携 |
| 2.4.0 | - | タイマーリロード復元機能 |
| 2.5.0 | - | プリセット時間変更（5/10/30/60分）、時間調整ボタン追加 |