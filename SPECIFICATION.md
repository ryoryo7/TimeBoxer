# タスク管理アプリ 仕様書

## 概要

シンプルな静的タスク管理アプリケーション。ローカルストレージを使用してブラウザにデータを永続化する。

## ファイル構成

```
task-manager/
├── index.html          # HTML構造
├── style.css           # スタイルシート
├── script.js           # JavaScript ロジック
└── SPECIFICATION.md    # 仕様書（本ファイル）
```

## 技術仕様

| 項目 | 内容 |
|------|------|
| フロントエンド | HTML5, CSS3, Vanilla JavaScript |
| データ保存 | LocalStorage |
| ストレージキー | `taskManager_tasks` |
| レスポンシブ | 対応（768px, 480px ブレークポイント） |

---

## データ構造

### タスクオブジェクト

ローカルストレージには、タスクオブジェクトの配列がJSON形式で保存される。

```javascript
{
    id: number,            // ユニークID（Date.now()で生成）
    name: string,          // タスク名
    priority: number,      // 優先順位（1以上の整数、一意、小さいほど高優先）
    estimatedTime: number, // 想定作業時間（分単位）
    targetDate: string,    // 対象日（YYYY-MM-DD形式）
    completed: boolean,    // 完了状態
    createdAt: string      // 作成日時（ISO 8601形式）
}
```

### サンプルデータ

```json
[
    {
        "id": 1703980800000,
        "name": "企画書作成",
        "priority": 1,
        "estimatedTime": 120,
        "targetDate": "2024-12-31",
        "completed": false,
        "createdAt": "2024-12-31T00:00:00.000Z"
    },
    {
        "id": 1703980900000,
        "name": "メール返信",
        "priority": 2,
        "estimatedTime": 15,
        "targetDate": "2024-12-30",
        "completed": false,
        "createdAt": "2024-12-31T00:01:00.000Z"
    }
]
```

---

## 画面構成

### 統計エリア

| 表示項目 | 説明 |
|----------|------|
| 総タスク数 | 登録されている全タスク数 |
| 未完了 | 完了していないタスク数 |
| 総想定時間 | 未完了タスクの想定時間合計（時間分形式で表示） |

### 入力フォーム

| フィールド | 型 | デフォルト値 | 制約 |
|------------|-----|--------------|------|
| タスク名 | text | - | 必須、空文字不可 |
| 優先順位 | number | 空（`--`） | 1以上の整数、未指定で最下位 |
| 想定時間(分) | number | 30 | 1以上の整数 |
| 対象日 | date | 今日 | - |

### タスク一覧テーブル

| カラム | 説明 | レスポンシブ時の表示 |
|--------|------|---------------------|
| ドラッグハンドル | 並び替え用（⋮⋮） | 常に表示 |
| チェックボックス | タスク選択用 | 常に表示 |
| 優先 | 優先順位（バッジ表示） | 常に表示 |
| タスク名 | タスクの名称 | 常に表示 |
| 想定時間 | 想定作業時間 | 480px以下で非表示 |
| 対象日 | タスク実施予定日（M/D形式） | 768px以下で非表示 |
| 操作 | 完了/編集/削除ボタン | 768px以下で非表示 |

### 対象日の色分け

| 状態 | 色 | CSSクラス |
|------|-----|-----------|
| 過去 | 赤 | `.past` |
| 今日 | 緑（太字） | `.today` |
| 未来 | 黒 | - |
| 完了タスク | 色分けなし | - |

### ツールバー（タスクリスト下部）

| ボタン | 機能 | 備考 |
|--------|------|------|
| CSVエクスポート | 全タスクをCSV形式でダウンロード | - |
| 選択削除 | チェックしたタスクを削除 | 確認ダイアログあり、選択がない場合は無効化 |
| 全削除 | 全タスクを削除 | 確認ダイアログあり |

---

## 機能一覧

### タスク追加
- タスク名、優先順位、想定時間、対象日を入力して追加
- Enterキーでも追加可能
- 追加後、フォームは初期値にリセット
- 対象日のデフォルトは今日
- 優先順位のデフォルトは空（未指定で最下位に追加）

### 優先順位の一意性
- 優先順位は1から連番で自動管理
- 同じ数字は存在しない
- タスク追加・削除・並び替え時に自動で再採番

### 優先順位の自動決定
- 優先順位未指定：既存の未完了タスクの最下位+1
- 優先順位指定：指定位置に挿入、それ以下は繰り下げ

### ドラッグ&ドロップ並び替え
- 未完了タスクのみドラッグ可能
- ドラッグハンドル（⋮⋮）をドラッグして並び替え
- ドロップ時に優先順位を自動再採番
- 完了タスクはドラッグ不可

### 自動ソート
- 優先順位の昇順（数字が小さいほど上）
- 完了タスクは未完了タスクの後ろに表示

### 完了切り替え
- チェックボタンで完了/未完了を切り替え
- 完了タスクは打ち消し線＋薄い緑背景で表示
- 完了/未完了切り替え時に優先順位を再採番

### タスク編集（インライン編集）
- 編集ボタン（✏️）をクリックで編集モード開始
- 編集可能項目: 優先順位、タスク名、想定時間、対象日
- 各フィールドがその場で入力欄に変化
- Enterキーで保存、Escキーでキャンセル
- 保存ボタン（✓）/ キャンセルボタン（✕）でも操作可能
- 同時に編集できるのは1件のみ
- 編集中は行の背景が水色に変化
- 優先順位変更時は自動で再採番

### タスク削除（個別）
- 削除ボタンで即座に削除
- 確認ダイアログなし
- 削除後に優先順位を再採番

### タスク選択
- 各タスクにチェックボックスあり
- ヘッダーの全選択チェックボックスで一括選択/解除
- 一部選択時は全選択チェックボックスが中間状態（indeterminate）

### 選択削除
- 選択したタスクを一括削除
- 確認ダイアログあり
- 選択がない場合はボタン無効化

### 全削除
- 全タスクを一括削除
- 確認ダイアログあり（件数表示）

### CSVエクスポート
- 全タスクをCSVファイルとしてダウンロード
- BOM付きUTF-8で出力（Excel対応）
- ファイル名: `tasks_YYYYMMDD_HHMMSS.csv`

---

## CSVフォーマット

### ヘッダー

```csv
タスク名,優先順位,想定時間(分),対象日,完了,作成日
```

### データ例

```csv
タスク名,優先順位,想定時間(分),対象日,完了,作成日
企画書作成,1,120,2024/12/31,未完了,2024/12/31
メール返信,2,15,2024/12/30,未完了,2024/12/31
```

### エスケープ処理
- カンマ、ダブルクォート、改行を含む場合はダブルクォートで囲む
- ダブルクォートは二重化（`"` → `""`）

---

## 優先順位バッジの色分け

優先順位は未完了タスク数に対する相対位置で色分け。

| 位置 | 色 | CSSクラス |
|------|-----|-----------|
| 上位1/3 | 赤系グラデーション | `.priority-high` |
| 中位1/3 | オレンジ系グラデーション | `.priority-medium` |
| 下位1/3 | 緑系グラデーション | `.priority-low` |
| 完了タスク | 緑系 | `.priority-low` |

---

## 関数一覧（script.js）

| 関数名 | 説明 |
|--------|------|
| `getTasks()` | ローカルストレージからタスク配列を取得 |
| `saveTasks(tasks)` | タスク配列をローカルストレージに保存 |
| `normalizePriorities(tasks)` | 優先順位を1から連番に再採番 |
| `getNewPriority(tasks, requestedPriority)` | 新規タスクの優先順位を決定 |
| `insertTaskAtPriority(tasks, newTask, priority)` | 指定位置にタスクを挿入し再採番 |
| `addTask()` | 新規タスクを追加 |
| `toggleComplete(id)` | 指定タスクの完了状態を切り替え |
| `deleteTask(id)` | 指定タスクを削除 |
| `startEdit(id)` | 指定タスクの編集モードを開始 |
| `cancelEdit()` | 編集をキャンセル |
| `saveEdit(id)` | 編集内容を保存 |
| `handleEditKeypress(e, id)` | 編集中のキーボード操作を処理 |
| `handleDragStart(e, id)` | ドラッグ開始処理 |
| `handleDragEnd(e)` | ドラッグ終了処理 |
| `handleDragOver(e, id)` | ドラッグオーバー処理 |
| `handleDrop(e, targetId)` | ドロップ処理 |
| `handleSelectAll()` | 全選択チェックボックスの処理 |
| `handleTaskSelect(id, checked)` | 個別チェックボックスの処理 |
| `updateSelectAllState()` | 全選択チェックボックスの状態更新 |
| `updateDeleteSelectedBtn()` | 選択削除ボタンの有効/無効更新 |
| `updateSelectedStyle()` | 選択行のスタイル更新 |
| `deleteSelected()` | 選択タスクを削除 |
| `deleteAll()` | 全タスクを削除 |
| `exportToCsv()` | CSVエクスポート実行 |
| `escapeCsvField(field)` | CSVフィールドのエスケープ |
| `formatDateForCsv(isoString)` | CSV用日付フォーマット（作成日） |
| `formatTargetDateForCsv(dateString)` | CSV用日付フォーマット（対象日） |
| `generateCsvFilename()` | CSVファイル名生成 |
| `setDefaultTargetDate()` | 対象日のデフォルト値設定 |
| `formatDateForInput(date)` | input[type="date"]用フォーマット |
| `formatTargetDate(dateString)` | 対象日の表示用フォーマット |
| `getTargetDateClass(dateString)` | 対象日の色分けクラス取得 |
| `formatTime(minutes)` | 分を `X時間X分` 形式に変換 |
| `getPriorityClass(priority, total)` | 優先順位に応じたCSSクラスを返す |
| `updateStats(tasks)` | 統計エリアを更新 |
| `escapeHtml(text)` | HTMLエスケープ処理 |
| `renderTaskRow(task, total)` | 通常表示のタスク行HTMLを生成 |
| `renderEditingTaskRow(task, total)` | 編集中のタスク行HTMLを生成 |
| `renderTasks()` | タスク一覧を再描画 |
| `setupEventListeners()` | イベントリスナーを設定 |

---

## レスポンシブ対応

| ブレークポイント | 変更内容 |
|------------------|----------|
| 768px以下 | 操作カラム非表示、フォーム縦並び、統計カード縦並び、ツールバー中央寄せ |
| 480px以下 | 想定時間カラム非表示、タイトルフォントサイズ縮小、ツールバーボタン縮小 |

---

## 更新履歴

| バージョン | 日付 | 内容 |
|------------|------|------|
| 1.0.0 | - | 初回リリース（タスク追加/完了/削除/ソート） |
| 1.1.0 | - | 想定時間を分単位に変更 |
| 1.2.0 | - | CSVエクスポート、選択削除、全削除機能追加 |
| 1.3.0 | - | 作成日をCSVのみに変更、対象日フィールド追加 |
| 1.4.0 | - | インライン編集機能追加 |
| 1.5.0 | - | 優先順位の一意化、ドラッグ&ドロップ並び替え機能追加 |
```

---

## 実装完了

| 機能 | 内容 |
|------|------|
| 優先順位の一意性 | 同じ数字なし、1から連番で自動管理 |
| 新規タスクのデフォルト | 優先順位欄は空（`--`表示） |
| 優先順位未指定時 | 最下位+1を自動付与 |
| 優先順位指定時 | 指定位置に挿入、以下を繰り下げ |
| ドラッグ&ドロップ | ⋮⋮ハンドルで並び替え、ドロップ時に再採番 |
| バッジ色 | 相対位置で色分け（上位1/3=赤、中位=橙、下位=緑） |