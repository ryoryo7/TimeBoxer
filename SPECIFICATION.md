# タスク管理アプリ 仕様書

## 概要

シンプルな静的タスク管理アプリケーション。ローカルストレージを使用してブラウザにデータを永続化する。タイマー機能と実行ログ機能を備える。

## ファイル構成

```
task-manager/
├── index.html          # HTML構造
├── style.css           # スタイルシート
├── script.js           # JavaScript ロジック
├── package.json        # npm設定
├── .prettierrc         # Prettier設定
├── .prettierignore     # Prettier除外設定
├── eslint.config.js    # ESLint設定
├── vitest.config.js    # Vitest設定
├── .gitignore          # Git除外設定
├── SPECIFICATION.md    # 仕様書（本ファイル）
├── SETUP_GUIDE.md      # 開発環境セットアップ指示書
└── tests/
    └── tasks.test.js   # ユニットテスト
```

## 技術仕様

| 項目 | 内容 |
|------|------|
| フロントエンド | HTML5, CSS3, Vanilla JavaScript |
| データ保存 | LocalStorage |
| テスト | Vitest |
| コード整形 | Prettier |
| 静的解析 | ESLint |
| レスポンシブ | 対応（768px, 480px ブレークポイント） |

---

## LocalStorageキー

| キー | 内容 |
|------|------|
| `taskManager_tasks` | タスクデータ |
| `taskManager_logs` | 実行ログデータ |

---

## データ構造

### タスクオブジェクト

```javascript
{
    id: number,            // ユニークID（Date.now()で生成）
    name: string,          // タスク名
    priority: number,      // 優先順位（1以上の整数、一意、小さいほど高優先）
    estimatedTime: number, // 想定作業時間（分単位）
    targetDate: string,    // 対象日（YYYY-MM-DD形式）
    completed: boolean,    // 完了状態
    createdAt: string      // 作成日時（ISO 8601形式）
}
```

### ログオブジェクト

```javascript
{
    id: number,              // ユニークID（Date.now()で生成）
    taskId: number|string,   // 対象タスクのID（割り込みタスクの場合は'interrupt'）
    taskName: string,        // タスク名（タスク削除後も表示するため保持）
    estimatedTime: number,   // 想定時間（分、割り込みタスクの場合は入力時間）
    startDateTime: string,   // 開始日時（YYYY-MM-DD HH:MM形式）
    endDateTime: string,     // 終了日時（YYYY-MM-DD HH:MM形式）
    duration: number,        // 実行時間（分）
    completed: boolean       // 最後まで完了したか（true=完了、false=中断）
}
```

### サンプルデータ

#### タスク

```json
[
    {
        "id": 1703980800000,
        "name": "企画書作成",
        "priority": 1,
        "estimatedTime": 120,
        "targetDate": "2024-12-31",
        "completed": false,
        "createdAt": "2024-12-31T00:00:00.000Z"
    }
]
```

#### ログ

```json
[
    {
        "id": 1703981000000,
        "taskId": 1703980800000,
        "taskName": "企画書作成",
        "estimatedTime": 120,
        "startDateTime": "2024-12-29 14:30",
        "endDateTime": "2024-12-29 14:55",
        "duration": 25,
        "completed": true
    },
    {
        "id": 1703981100000,
        "taskId": "interrupt",
        "taskName": "割り込みタスク",
        "estimatedTime": 15,
        "startDateTime": "2024-12-29 15:00",
        "endDateTime": "2024-12-29 15:10",
        "duration": 10,
        "completed": false
    }
]
```

---

## 画面構成

### 1. 統計エリア

| 表示項目 | 説明 |
|----------|------|
| 総タスク数 | 登録されている全タスク数 |
| 未完了 | 完了していないタスク数 |
| 総想定時間 | 未完了タスクの想定時間合計（時間分形式で表示） |

### 2. 入力フォーム

| フィールド | 型 | デフォルト値 | 制約 |
|------------|-----|--------------|------|
| タスク名 | text | - | 必須、空文字不可 |
| 優先順位 | number | 空（`--`） | 1以上の整数、未指定で最下位 |
| 想定時間(分) | number | 30 | 1以上の整数 |
| 対象日 | date | 今日 | - |

### 3. タスク一覧テーブル

| カラム | 説明 | レスポンシブ時の表示 |
|--------|------|---------------------|
| ドラッグハンドル | 並び替え用（⋮⋮） | 常に表示 |
| チェックボックス | タスク選択用 | 常に表示 |
| 優先 | 優先順位（バッジ表示） | 常に表示 |
| タスク名 | タスクの名称 | 常に表示 |
| 想定時間 | 想定作業時間 | 480px以下で非表示 |
| 対象日 | タスク実施予定日（M/D形式） | 768px以下で非表示 |
| 操作 | 完了/編集/削除ボタン | 768px以下で非表示 |

### 4. ツールバー（タスクリスト下部）

| ボタン | 機能 | 備考 |
|--------|------|------|
| CSVエクスポート | タスク + ログをCSV形式でダウンロード | - |
| 選択削除 | チェックしたタスクを削除 | 確認ダイアログあり |
| 全削除 | 全タスクを削除 | 確認ダイアログあり |

### 5. タイマーセクション

| 要素 | 説明 |
|------|------|
| タスク選択 | ドロップダウン（デフォルト: 割り込みタスク） |
| プリセットボタン | 15分/25分/50分 |
| カスタム入力 | 任意の分数を入力 |
| タイマー表示 | 残り時間（MM:SS形式） |
| スケジュール表示 | 開始時刻 → 終了予定時刻 |
| スタートボタン | カウントダウン開始 |
| ストップボタン | 一時停止（ログ保存） |
| クリアボタン | タイマーリセット |

### 6. 実行ログセクション

| カラム | 説明 | レスポンシブ時の表示 |
|--------|------|---------------------|
| 実行日時 | 開始 ~ 終了（YYYY-MM-DD HH:MM形式） | 常に表示 |
| タスク名 | 実行したタスク名（✓=完了、⏸=中断） | 480px以下で非表示 |
| 想定時間 | タスクに設定された想定時間 | 768px以下で非表示 |
| 実行時間 | 今回の経過時間 | 常に表示 |
| 合計 | そのログまでの累計実行時間 | 常に表示 |

---

## 機能一覧

### タスク管理機能

#### タスク追加
- タスク名、優先順位、想定時間、対象日を入力して追加
- Enterキーでも追加可能
- 追加後、フォームは初期値にリセット

#### 優先順位の一意性
- 優先順位は1から連番で自動管理
- 同じ数字は存在しない

#### ドラッグ&ドロップ並び替え
- 未完了タスクのみドラッグ可能
- ドロップ時に優先順位を自動再採番

#### 完了切り替え
- チェックボタンで完了/未完了を切り替え
- 完了タスクは打ち消し線＋薄い緑背景

#### タスク編集（インライン編集）
- 編集ボタン（✏️）で編集モード開始
- Enterで保存、Escでキャンセル

---

### タイマー機能

#### タスク選択
- デフォルト: 「割り込みタスク」（タスク未登録でも使用可能）
- 未完了タスクもドロップダウンに表示
- タスク選択時、想定時間が自動セット

#### 割り込みタスク
- タスクリストに登録せずにタイマーを使用可能
- 想定時間は入力した時間がそのまま記録される
- ログには「割り込みタスク」として保存

#### 時間設定
- プリセット: 15分/25分/50分ボタン
- カスタム: 1〜999分の任意入力

#### スタート
- 時間未入力時はエラー
- 開始時刻を記録
- 終了予定時刻を計算・表示

#### ストップ（一時停止）
- カウントダウン一時停止
- **スタート〜ストップ時刻をログに保存**（1分以上経過時）
- ログのcompleted: false（中断）として記録
- スタートボタンで再開可能（新しい開始時刻で記録）

#### クリア
- カウントダウン停止
- ログは保存しない
- タイマーを初期状態に戻す
- タスク選択を「割り込みタスク」にリセット

#### 終了（時間経過）
- 終了時刻を記録
- ログのcompleted: true（完了）として保存
- 通知音＋画面メッセージ＋ブラウザ通知

---

### 実行ログ機能

#### ログ保存タイミング
- タイマー完了時（completed: true）
- ストップ時（completed: false、1分以上経過時のみ）
- クリア時は保存しない

#### ログ表示
- ✓: 完了
- ⏸: 中断（ストップ）
- 累計時間は上から順に加算

---

### CSVエクスポート機能

#### CSVフォーマット

```csv
=== タスク一覧 ===
タスク名,優先順位,想定時間(分),対象日,完了,作成日
企画書作成,1,120,2024/12/31,未完了,2024/12/31

=== 実行ログ ===
開始日時,終了日時,タスク名,想定時間(分),実行時間(分),累計時間(分),完了
2024-12-29 14:30,2024-12-29 14:55,企画書作成,120,25,25,完了
2024-12-29 15:00,2024-12-29 15:10,割り込みタスク,15,10,35,中断
```

---

## 表示仕様

### 対象日の色分け

| 状態 | 色 | CSSクラス |
|------|-----|-----------|
| 過去 | 赤 | `.past` |
| 今日 | 緑（太字） | `.today` |
| 未来 | 黒 | - |

### 優先順位バッジの色分け

| 位置 | 色 | CSSクラス |
|------|-----|-----------|
| 上位1/3 | 赤系 | `.priority-high` |
| 中位1/3 | オレンジ系 | `.priority-medium` |
| 下位1/3 | 緑系 | `.priority-low` |

### 時間表示フォーマット

| 条件 | 表示形式 | 例 |
|------|----------|-----|
| 60分未満 | X分 | 30分 |
| 60分ちょうど | X時間 | 1時間 |
| 60分以上 | X時間Y分 | 1時間30分 |

---

## 状態遷移

### タイマー状態

```
[初期状態] (割り込みタスク選択済み)
    │
    ├─ スタート → [実行中]
    │               │
    │               ├─ 時間経過 → [完了] → ログ保存(completed:true) → [初期状態]
    │               │
    │               └─ ストップ → ログ保存(completed:false) → [停止中]
    │                               │
    │                               ├─ スタート → [実行中] (新しい開始時刻)
    │                               │
    │                               └─ クリア → [初期状態]
    │
    └─ クリア → [初期状態]
```

---

## 制約事項

### タスク

| 項目 | 制約 |
|------|------|
| タスク名 | 必須、空文字不可 |
| 優先順位 | 1以上の整数、一意 |
| 想定時間 | 1分以上 |

### タイマー

| 項目 | 制約 |
|------|------|
| タスク選択 | デフォルト: 割り込みタスク |
| 時間設定 | 1〜999分 |
| ストップ時のログ保存 | 1分以上経過時のみ |

---

## 更新履歴

| バージョン | 日付 | 内容 |
|------------|------|------|
| 1.0.0 | - | 初回リリース |
| 1.1.0 | - | 想定時間を分単位に変更 |
| 1.2.0 | - | CSVエクスポート、選択削除、全削除機能追加 |
| 1.3.0 | - | 対象日フィールド追加 |
| 1.4.0 | - | インライン編集機能追加 |
| 1.5.0 | - | 優先順位の一意化、ドラッグ&ドロップ並び替え |
| 1.6.0 | - | タイマー機能、実行ログ機能追加 |
| 1.7.0 | - | 割り込みタスクをデフォルトに、ストップ時ログ保存 |
```

---

## 変更まとめ

| 項目 | 内容 |
|------|------|
| タスク選択デフォルト | 「割り込みタスク」が常に先頭・選択済み |
| ストップ時 | スタート〜ストップ時刻をログに保存（1分以上） |
| ログ表示 | ✓=完了、⏸=中断のアイコン表示 |
| CSV出力 | 完了/中断のステータスカラム追加 |
| 再開時 | 新しい開始時刻で記録（別ログとして保存） |

## メモ帳機能

### 概要
複数のメモをタブ形式で管理できるメモ帳機能。

### 配置場所
ヘッダーの直下、統計エリアの上

### LocalStorageキー
- `taskManager_memos`

### データ構造
```javascript
{
  memos: [
    { id: number, name: string, content: string }
  ],
  activeTabId: number,
  isCollapsed: boolean
}

## タスク別時間割合グラフ

### 概要
実行ログのデータを元に、タスク別の実行時間割合を円グラフで可視化する。

### 配置場所
実行ログセクションの下

### 使用ライブラリ
- Chart.js（CDN）

### 機能
| 機能 | 説明 |
|------|------|
| 円グラフ表示 | タスク別の実行時間割合を表示 |
| 期間フィルター | 今日/今週/今月/全期間 を選択可能 |
| 1%未満の集約 | 1%未満のタスクは「その他」にまとめる |
| ツールチップ | ホバーで詳細（タスク名、割合、時間）を表示 |
| ラベル表示 | 5%以上のセグメントに割合と時間を表示 |

### 表示データ
| 項目 | 内容 |
|------|------|
| タスク名 | ログに記録されたタスク名 |
| 実行時間 | タスク別の合計実行時間（分） |
| 割合 | 全体に対するパーセンテージ |

### データがない場合
「実行ログがありません」と表示
